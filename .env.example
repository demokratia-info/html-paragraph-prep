PORT=8787
ALLOWED_ORIGIN=https://demokratia-info.github.io,http://localhost:8080,http://127.0.0.1:8080

# For local-only use, the API accepts any non-empty editor password if both
# values below are empty. Set one before exposing the API through a tunnel.
# EDITOR_PASSWORD=
# EDITOR_PASSWORD_SHA256=
